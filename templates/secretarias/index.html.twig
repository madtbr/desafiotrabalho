{% extends 'base.html.twig' %}

{% block title %}Secretarias index{% endblock %}

{% block body %}
    <h1>Secretarias index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for secretaria in secretarias %}
            <tr>
                <td>{{ secretaria.id }}</td>
                <td>{{ secretaria.nome }}</td>
                <td>
                    <a href="{{ path('secretarias_show', {'id': secretaria.id}) }}">show</a>
                    <a href="{{ path('secretarias_edit', {'id': secretaria.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('secretarias_new') }}">Create new</a>
{% endblock %}
